apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-toolki
  labels:
    app: devops-toolkit
spec:
  selector:
    matchLabels:
      app: devops-toolkit
  template:
    metadata:
      labels:
        app: devops-toolkit
    spec:
      containers:
        - name: devops-toolkit
          image: sreenathkk96/hello
          ports:
            - containerPort: 80